<%= form_for(@auother) do |f| %>
  <% if @auother.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auother.errors.count, "error") %> prohibited this auother from being saved:</h2>

      <ul>
      <% @auother.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
